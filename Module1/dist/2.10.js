"use strict";
{
    // mapped typed
    const arrayOfNumbers = [1, 2, 3];
    const arrOfString = arrayOfNumbers.map((num) => num.toString());
    console.log(arrOfString);
    const area1 = {
        height: "100",
        width: 50
    };
}
