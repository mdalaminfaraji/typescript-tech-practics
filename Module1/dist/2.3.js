"use strict";
{
    // amra reusable korte partechi
    // const rollNumbers:number[]=[3, 4, 5];
    const rollNumbers = [3, 4, 5];
    // const montors:string[]=["mr", 'x'];
    const montors = ["mr", 'x'];
    // const boolArray:boolean[]=[true, false, true];
    const boolArray = [true, false, true];
    const user = [
        {
            name: "Mezba",
            age: 100
        },
        {
            name: "alamin",
            age: 234
        },
    ];
    const add = (x, y) => x + y;
    const manush = ['mrx', 345];
    const UserWithID = [34, { name: 'alamin', Age: 234, role: "admin" }];
}
